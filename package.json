{
  "name": "calculator-widget",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@lit-labs/task": "^2.0.0",
    "lit": "^2.6.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "build:widget": "parcel build src/index.html --no-source-maps --dist-dir dist --public-url ./",
    "serve:widget": "parcel serve src/index.html --dist-dir build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "parcel": "^2.0.0-nightly.1236",
    "postcss-modules": "^6.0.0"
  }
}
